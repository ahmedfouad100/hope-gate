# Leave unset for local dev (vite.config.js proxies /api to localhost:8000).
# In production, set to your deployed backend's URL, e.g.:
# VITE_API_URL=https://hope-gate-backend.onrender.com/api
VITE_API_URL=
